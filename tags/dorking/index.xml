<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Dorking on Ef&#39;s log</title>
    <link>https://fahmifj.github.io/tags/dorking/</link>
    <description>Recent content in Dorking on Ef&#39;s log</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <lastBuildDate>Mon, 14 Jun 2021 14:41:04 +0700</lastBuildDate><atom:link href="https://fahmifj.github.io/tags/dorking/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Install jarsigner Without Java SDK in Kali Linux</title>
      <link>https://fahmifj.github.io/blog/install-jar-signer-without-java-sdk/</link>
      <pubDate>Mon, 14 Jun 2021 14:41:04 +0700</pubDate>
      
      <guid>https://fahmifj.github.io/blog/install-jar-signer-without-java-sdk/</guid>
      <description>You can use Google dork for good things!</description>
      <content:encoded><![CDATA[<p>The PoC exploit of <a href="/ctf/hackthebox/htb-scriptkiddie/"target="_blank" rel="noopener noreferrer"
>ScriptKiddie</a> from HackTheBox actually requires a binary known as <code>jarsigner</code> . The binary typically bundled with the Java SDK. Since Kali Linux ships with several Java-based applications, I assumed that the binary was already installed somewhere.</p>
<p>However, I was unable to locate this <code>jarsigner</code>.</p>
<h2 id="the-problem">The Problem</h2>
<p>I thought about reinstalling the Java SDK just to obtain this single binary file felt unnecessary. I mean, it could potentially affect the existing Java environment, so I started looking for the binary online.</p>
<p>Sadly, most of the search results were just a bunch of documentation about how to use <code>jarsigner</code>, rathen than where to obtain it directly.</p>
<h2 id="finding-solutions">Finding Solutions</h2>
<h3 id="in-search-of-the-missing-binary">In search of the missing binary</h3>
<p>One approach for this is to leverage Google dork to locate accessible directory listings containing the binary.</p>
<p>Here&rsquo;s my dork:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">intitle:&#34;index of /&#34; bin jarsigner</span></span></code></pre>
</figure><p>After digging trhough several pages, I came across a <a href="http://www.citrucoop.es/jdk-11.0.6/bin/"target="_blank" rel="noopener noreferrer"
>site</a> hosting an entire Java JDK folder.</p>
<p><div class="img-container"><img src="imgs/image-20210614151715867.png" alt="image-20210614151715867"  /></div>
</p>
<p>Among the files listed, there was the <code>jarsigner</code> file I was looking for. So I pulled that file to my local folder.</p>
<h3 id="installing-jarsigner">Installing jarsigner</h3>
<p>Unfortunately, the file can not be used yet by just simply putting the binary into the Java PATH. This binary needs to be &lsquo;symlinked&rsquo;.</p>
<p>So, first I&rsquo;ll have to locate where is the entire Java binaries located.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ ls -l <span class="k">$(</span>which java<span class="k">)</span>
</span></span><span class="line"><span class="cl">lrwxrwxrwx <span class="m">1</span> root root <span class="m">22</span> Nov <span class="m">25</span>  <span class="m">2019</span> /usr/bin/java -&gt; /etc/alternatives/java
</span></span><span class="line"><span class="cl">$ ls -l /etc/alternatives/java
</span></span><span class="line"><span class="cl">lrwxrwxrwx <span class="m">1</span> root root <span class="m">43</span> Nov <span class="m">25</span>  <span class="m">2019</span> /etc/alternatives/java -&gt; /usr/lib/jvm/java-11-openjdk-amd64/bin/java</span></span></code></pre>
</figure><p>Next, I&rsquo;ll grab the <code>jarsigner</code> binary from the previous site and drop it directly under <code>/usr/lib/jvm/java-11-openjdk-amd64/bin/</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ curl -s http://www.citrucoop.es/jdk-11.0.6/bin/jarsigner &gt; /usr/lib/jvm/java-11-openjdk-amd64/bin/jarsigner</span></span></code></pre>
</figure><p>Lastly, I&rsquo;ll make a symlink of <code>jarsigner</code> at <code>/usr/bin/</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ ln -sf /usr/lib/jvm/java-11-openjdk-amd64/bin/jarsigner /usr/bin/jarsigner</span></span></code></pre>
</figure><p>The exploit for ScriptKiddie should be working again now!</p>
<p><em>Hold up.. isn&rsquo;t it unsafe?</em> 🤔</p>
<p>Absolutely! 😅 downloading binaries from an unknown server is a bad idea, sdon&rsquo;t ever do this if you don&rsquo;t trust the site or don&rsquo;t know what you are doing!</p>
]]></content:encoded>
    </item>
    
  </channel>
</rss>
