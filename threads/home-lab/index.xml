<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Home Lab on Ef&#39;s log</title>
    <link>https://fahmifj.github.io/threads/home-lab/</link>
    <description>Recent content in Home Lab on Ef&#39;s log</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <lastBuildDate>Tue, 09 Jun 2026 17:08:00 +0700</lastBuildDate><atom:link href="https://fahmifj.github.io/threads/home-lab/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>PNETLab on Google Cloud – Part 1: Server Deployment</title>
      <link>https://fahmifj.github.io/blog/deploy-pnetlab-on-gcp-part-1/</link>
      <pubDate>Wed, 26 Mar 2025 13:27:29 +0700</pubDate>
      
      <guid>https://fahmifj.github.io/blog/deploy-pnetlab-on-gcp-part-1/</guid>
      <description>Introduction While running GNS3 inside Orbstack, I encountered an issue with Orbstack&amp;rsquo;s networking system that prevented my localhost from communicating with GNS3 appliances. Instead of spending more time troubleshooting something that is limited by its design, I decided to deploy a cloud-hosted network lab using PNETLab.
The lab infrastructure is provisioned using Terraform and remote access to the lab is secured through Tailscale instead of exposing it directly to the Internet.</description>
      <content:encoded><![CDATA[<h2 id="introduction">Introduction</h2>
<p>While running <a href="/articles/running-gns3-on-apple-m2-orbstack/"target="_blank" rel="noopener noreferrer"
>GNS3 inside Orbstack</a>, I encountered an issue with Orbstack&rsquo;s networking system that prevented my localhost from communicating with GNS3 appliances. Instead of spending more time troubleshooting something that is limited by its design, I decided to deploy a cloud-hosted network lab using PNETLab.</p>
<p>The lab infrastructure is provisioned using Terraform and remote access to the lab is secured through Tailscale instead of exposing it directly to the Internet.</p>
<p>This article covers the deployment process. The remote access setup will be covered in a separate article.</p>
<h3 id="goals">Goals</h3>
<ul>
<li>Build reusable PNETLab server with Terraform.</li>
</ul>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>GCP &amp; Tailscale accounts.</li>
<li>GCP CLI installed.</li>
<li>Terraform installed.</li>
</ul>
<h2 id="architecture-overview"><strong>Architecture Overview</strong></h2>
<p>Here’s an overview of what we’re going to build:</p>
<img src="./imgs/image-20251028001404879.png" alt="image-20251028001404879" style="zoom:50%;" />
<p>The setup consists of:</p>
<ul>
<li><strong>Terraform for infrastructure automation</strong>.</li>
<li><strong>A GCP compute instance running PNETLab</strong>.</li>
<li><strong>A private VPC network</strong>.</li>
<li><strong>Cloud Router for outbound internet access (dynamic IP)</strong>.</li>
<li>Tailscale for secure remote access to PNETLab instance</li>
</ul>
<h2 id="prepare-the-gcp-environment">Prepare the GCP Environment</h2>
<p>Before we begin, several prerequisites need to be prepared within Google Cloud Platform.</p>
<blockquote>
<p>Note: I&rsquo;m using GCP&rsquo;s free trial credits for this setup, if you&rsquo;re on a paid account, the steps remain the same.</p>
</blockquote>
<h3 id="gcp-authentication">GCP Authentication</h3>
<p>Terraforms requires credentials to access your Google Cloud resources. There are several methods for this but I will be using Application Default Credentials (ADC) method for this personal project.</p>
<p>From CLI, run the following:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ gcloud auth application-default login</span></span></code></pre>
</figure><p>A browser window will pop ups, requesting for Google account authentication. After a successful authtentication, the credentials will be stored in the default location and Terraform will look into it automatically.</p>
<h3 id="creating-a-gcp-project">Creating a GCP Project</h3>
<p>First, create a new project for our PNETLab deployment using GCP CLI.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ <span class="nv">PROJECT_ID</span><span class="o">=</span>pnetlab-<span class="nv">$RANDOM</span>
</span></span><span class="line"><span class="cl">$ <span class="nb">echo</span> <span class="nv">$PROJECT_ID</span>
</span></span><span class="line"><span class="cl">pnetlab-xxx
</span></span><span class="line"><span class="cl">$ mkdir <span class="nv">$PROJECT_ID</span>
</span></span><span class="line"><span class="cl">$ <span class="nb">cd</span> <span class="nv">$PROJECT_ID</span>
</span></span><span class="line"><span class="cl">$ gcloud projects create <span class="nv">$PROJECT_ID</span>
</span></span><span class="line"><span class="cl">$ gcloud config <span class="nb">set</span> project <span class="nv">$PROJECT_ID</span></span></span></code></pre>
</figure><p>Then, link the project to your <a href="https://cloud.google.com/billing/docs/how-to/create-billing-account"target="_blank" rel="noopener noreferrer"
>billing account</a>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ gcloud billing accounts list              
</span></span><span class="line"><span class="cl">ACCOUNT_ID            NAME                OPEN  MASTER_ACCOUNT_ID
</span></span><span class="line"><span class="cl">AAAAAA-BBBBBB-CCCCCC  My Billing Account  True
</span></span><span class="line"><span class="cl">$ gcloud billing projects link <span class="nv">$PROJECT_ID</span> --billing-account<span class="o">=</span>AAAAAA-BBBBBB-CCCCCC</span></span></code></pre>
</figure><h3 id="enabling-compute-engine-api">Enabling Compute Engine API</h3>
<p>To allow Terraform to manage VM resources, we must enable the Compute Engine API.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ gcloud services <span class="nb">enable</span> compute.googleapis.com</span></span></code></pre>
</figure><h2 id="server-deployment-with-terraform">Server Deployment with Terraform</h2>
<p>We will use Terraform to provision our server that will host PNETLab.</p>
<h3 id="preparing-project-structure">Preparing Project Structure</h3>
<p>Create the following project structure.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ tree
</span></span><span class="line"><span class="cl">.
</span></span><span class="line"><span class="cl">├── README.MD
</span></span><span class="line"><span class="cl">└── terraform/
</span></span><span class="line"><span class="cl">    ├── terraforms.tfvars <span class="c1"># sensitive informations goes here (project_id, region, user/keys)</span>
</span></span><span class="line"><span class="cl">    ├── main.tf <span class="c1"># defined gcp resources</span>
</span></span><span class="line"><span class="cl">    └── variables.tf
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="m">1</span> directories, <span class="m">4</span> files</span></span></code></pre>
</figure><h4 id="terraformtfvars">terraform.tfvars</h4>
<p><code>terraforms.tfvars</code> contains the following:</p>
<figure class="highlight">
    <pre tabindex="0"><code class="language-" data-lang="">project_id      = &#34;&#34;
region          = &#34;&#34;
zone            = &#34;&#34;
os_image        = &#34;projects/ubuntu-os-cloud/global/images/ubuntu-1804-bionic-v20240116a&#34;</code></pre>
</figure><p>Other than <code>os_image</code>, make your adjustment. We use older Ubuntu because the PNETLab core service</p>
<h4 id="maintf">main.tf</h4>
<p><code>main.tf</code> file contains the following:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="err">terraform</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">required_providers</span> <span class="err">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">google</span> <span class="err">=</span> <span class="err">{</span>
</span></span><span class="line"><span class="cl">      <span class="err">source</span> <span class="err">=</span> <span class="nt">&#34;hashicorp/google&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="err">version</span> <span class="err">=</span> <span class="s2">&#34;~&gt; 4.5&#34;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="err">}</span>
</span></span><span class="line"><span class="cl"><span class="err">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">provider</span> <span class="s2">&#34;google&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">project</span> <span class="err">=</span> <span class="err">var.project_id</span>
</span></span><span class="line"><span class="cl">  <span class="err">region</span>  <span class="err">=</span> <span class="err">var.region</span>
</span></span><span class="line"><span class="cl">  <span class="err">zone</span>    <span class="err">=</span> <span class="err">var.zone</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">#</span> <span class="err">SETUP</span> <span class="err">NETWORK</span> <span class="err">#</span>
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_network&#34;</span> <span class="s2">&#34;pnetlab_vpc&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span>                    <span class="err">=</span> <span class="nt">&#34;pnetlab-vpc&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">auto_create_subnetworks</span> <span class="err">=</span> <span class="kc">false</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_subnetwork&#34;</span> <span class="s2">&#34;pnetlab_subnet&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span>          <span class="err">=</span> <span class="nt">&#34;pnetlab-subnet&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">network</span>       <span class="err">=</span> <span class="err">google_compute_network.pnetlab_vpc.id</span>
</span></span><span class="line"><span class="cl">  <span class="err">ip_cidr_range</span> <span class="err">=</span> <span class="s2">&#34;10.10.1.0/24&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_firewall&#34;</span> <span class="s2">&#34;allow_ssh_access&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span>    <span class="err">=</span> <span class="nt">&#34;allow-ssh&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">network</span> <span class="err">=</span> <span class="err">google_compute_network.pnetlab_vpc.id</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="err">allow</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">protocol</span> <span class="err">=</span> <span class="nt">&#34;tcp&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="err">ports</span>    <span class="err">=</span> <span class="p">[</span><span class="s2">&#34;22&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="err">source_ranges</span> <span class="err">=</span> <span class="p">[</span> <span class="s2">&#34;0.0.0.0/0&#34;</span> <span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">#</span> <span class="err">NAT</span>
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_router&#34;</span> <span class="s2">&#34;pnetlab_router&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span> <span class="err">=</span> <span class="nt">&#34;pnetlab-router&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">region</span> <span class="err">=</span> <span class="err">var.region</span>
</span></span><span class="line"><span class="cl">  <span class="err">network</span> <span class="err">=</span> <span class="err">google_compute_network.pnetlab_vpc.id</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_router_nat&#34;</span> <span class="s2">&#34;pnetlab_router_nat&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span> <span class="err">=</span> <span class="nt">&#34;pnetlab-router-nat&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">region</span> <span class="err">=</span> <span class="err">var.region</span>
</span></span><span class="line"><span class="cl">  <span class="err">router</span> <span class="err">=</span> <span class="err">google_compute_router.pnetlab_router.name</span>
</span></span><span class="line"><span class="cl">  <span class="err">nat_ip_allocate_option</span> <span class="err">=</span> <span class="s2">&#34;AUTO_ONLY&#34;</span> <span class="err">#</span> <span class="err">use</span> <span class="err">dynamic</span> <span class="err">public</span> <span class="err">ip</span> <span class="err">for</span> <span class="err">nat</span>
</span></span><span class="line"><span class="cl">  <span class="err">source_subnetwork_ip_ranges_to_nat</span> <span class="err">=</span> <span class="s2">&#34;ALL_SUBNETWORKS_ALL_IP_RANGES&#34;</span> <span class="err">#</span> <span class="err">nat</span> <span class="err">any</span> <span class="err">sources</span> <span class="err">ip</span> <span class="err">range</span> <span class="err">available</span> <span class="err">within</span> <span class="err">the</span> <span class="err">vpc</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">#</span> <span class="err">SETUP</span> <span class="err">EXT</span> <span class="err">DISK</span> <span class="err">#</span>
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_disk&#34;</span> <span class="s2">&#34;additional_disk&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span>   <span class="err">=</span> <span class="nt">&#34;pnetlab-additional-disk&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">type</span>   <span class="err">=</span> <span class="s2">&#34;pd-standard&#34;</span>  <span class="err">#</span> <span class="err">Choose</span> <span class="err">disk</span> <span class="err">type</span> <span class="err">(pd-standard</span><span class="p">,</span> <span class="err">pd-ssd,</span> <span class="err">etc.)</span>
</span></span><span class="line"><span class="cl">  <span class="err">size</span> <span class="err">=</span> <span class="err">50</span> 
</span></span><span class="line"><span class="cl">  <span class="err">lifecycle</span> <span class="err">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">prevent_destroy</span> <span class="err">=</span> <span class="err">true</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="err">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">#</span> <span class="err">SETUP</span> <span class="err">INSTANCES</span> <span class="err">#</span>
</span></span><span class="line"><span class="cl"><span class="err">#</span> <span class="err">ubuntu</span><span class="mi">-1804</span><span class="err">-bionic-v</span><span class="mi">20240116</span><span class="err">a</span>                                 <span class="err">ubuntu-os-cloud</span>      <span class="err">ubuntu</span><span class="mi">-1804</span><span class="err">-lts</span>
</span></span><span class="line"><span class="cl"><span class="err">resource</span> <span class="s2">&#34;google_compute_instance&#34;</span> <span class="s2">&#34;pnetlab_server&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">name</span>         <span class="err">=</span> <span class="nt">&#34;pnetlab-server&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">machine_type</span> <span class="err">=</span> <span class="s2">&#34;n2-standard-2&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">tags</span>         <span class="err">=</span> <span class="p">[</span><span class="s2">&#34;pnetlab&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="err">boot_disk</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">initialize_params</span> <span class="err">{</span>
</span></span><span class="line"><span class="cl">      <span class="err">image</span> <span class="err">=</span> <span class="err">var.os_image</span> <span class="err">#</span> <span class="err">cloud</span> <span class="err">image</span>
</span></span><span class="line"><span class="cl">      <span class="err">size</span>  <span class="err">=</span> <span class="err">20</span>                 <span class="err">#</span> <span class="err">assign</span> <span class="err">20gb</span>
</span></span><span class="line"><span class="cl">      <span class="err">type</span>  <span class="err">=</span> <span class="nt">&#34;pd-standard&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl">  
</span></span><span class="line"><span class="cl">  <span class="err">#</span> <span class="err">external</span> <span class="err">disk</span> <span class="mi">50</span><span class="err">gb</span>
</span></span><span class="line"><span class="cl">  <span class="err">attached_disk</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">source</span> <span class="err">=</span> <span class="err">google_compute_disk.additional_disk.id</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="err">network_interface</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">network</span>    <span class="err">=</span> <span class="err">google_compute_network.pnetlab_vpc.id</span>
</span></span><span class="line"><span class="cl">    <span class="err">subnetwork</span> <span class="err">=</span> <span class="err">google_compute_subnetwork.pnetlab_subnet.id</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="err">#</span> <span class="err">Disable</span> <span class="err">public</span> <span class="err">IP</span>
</span></span><span class="line"><span class="cl">    <span class="err">#</span> <span class="err">access_config</span> <span class="err">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">#</span>  <span class="err">network_tier</span> <span class="err">=</span> <span class="nt">&#34;STANDARD&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="err">#</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="err">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="err">scheduling</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">automatic_restart</span>           <span class="err">=</span> <span class="err">true</span>
</span></span><span class="line"><span class="cl">    <span class="err">on_host_maintenance</span>         <span class="err">=</span> <span class="nt">&#34;MIGRATE&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="err">preemptible</span>                 <span class="err">=</span> <span class="kc">false</span>
</span></span><span class="line"><span class="cl">    <span class="err">provisioning_model</span>          <span class="err">=</span> <span class="s2">&#34;STANDARD&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="err">advanced_machine_features</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="err">enable_nested_virtualization</span> <span class="err">=</span> <span class="err">true</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl">  
</span></span><span class="line"><span class="cl"><span class="err">}</span></span></span></code></pre>
</figure><p>This configuration provisions:</p>
<ul>
<li>Custom VPC network.</li>
<li>Subnet.</li>
<li>Firewall rules.</li>
<li>Cloud NAT router.</li>
<li>Additional persistent disk.</li>
<li>Enabled nested virtualization (need machine type that support nested virtualization).</li>
</ul>
<p>There&rsquo;s also n1 for alternative machine type if you worry about the cost, lets say <code>n1-custom-1-2048</code>, which means machine type is <code>n1</code> with single core and  2 GB of RAM.</p>
<h4 id="variablestf">variables.tf</h4>
<p><code>variables.tf</code> define the variables we used in the <code>tfvars</code> file, it contains:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="err">variable</span> <span class="s2">&#34;project_id&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">type</span>        <span class="err">=</span> <span class="err">string</span>
</span></span><span class="line"><span class="cl">  <span class="err">description</span> <span class="err">=</span> <span class="nt">&#34;Project ID&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">variable</span> <span class="s2">&#34;region&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">type</span>        <span class="err">=</span> <span class="err">string</span>
</span></span><span class="line"><span class="cl">  <span class="err">description</span> <span class="err">=</span> <span class="nt">&#34;Project region&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">default</span>     <span class="err">=</span> <span class="s2">&#34;us-central&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">variable</span> <span class="s2">&#34;zone&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">type</span>        <span class="err">=</span> <span class="err">string</span>
</span></span><span class="line"><span class="cl">  <span class="err">description</span> <span class="err">=</span> <span class="nt">&#34;Project zone&#34;</span>
</span></span><span class="line"><span class="cl">  <span class="err">default</span>     <span class="err">=</span> <span class="s2">&#34;us-central1-a&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">variable</span> <span class="s2">&#34;os_image&#34;</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="err">type</span>        <span class="err">=</span> <span class="err">string</span>
</span></span><span class="line"><span class="cl">  <span class="err">description</span> <span class="err">=</span> <span class="nt">&#34;OS family&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span></span></span></code></pre>
</figure><h3 id="vm-deployment">VM Deployment</h3>
<p>Within the terraform folder, initialize the terraform working directory.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ terraform init</span></span></code></pre>
</figure><p>The init step only needs to be performed be performed during the first deployment or after provider/module changes.</p>
<p>With all the resource files defined, we can start the deployment.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ terraform fmt         <span class="c1"># Format your Terraform files  </span>
</span></span><span class="line"><span class="cl">$ terraform validate    <span class="c1"># Check for syntax errors  </span>
</span></span><span class="line"><span class="cl">$ terraform plan -out pnetlab-deploy       <span class="c1">#Preview the infrastructure changes  </span>
</span></span><span class="line"><span class="cl">$ terraform apply pnetlab-deploy  <span class="c1"># Deploy the resources  </span></span></span></code></pre>
</figure><h3 id="verify-deployment">Verify Deployment</h3>
<p>After it finishes, check if the instance is running:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ gcloud compute instances list --filter<span class="o">=</span><span class="s2">&#34;name=pnetlab-server&#34;</span> --project<span class="o">=</span><span class="s2">&#34;</span><span class="nv">$PROJECT_ID</span><span class="s2">&#34;</span></span></span></code></pre>
</figure><blockquote>
<p>Please mind that the terraform config I provide here is only reusable for personal project, it&rsquo;s not reusable for team scale project.</p>
</blockquote>
<h2 id="pnetlab-installation">PNETLab Installation</h2>
<h3 id="install-pnetlab">Install PNETLab</h3>
<p>SSH into the instance using <code>gcloud</code> cli.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ gcloud compute ssh INSTANCE_NAME --zone<span class="o">=</span>YOUR_ZONE</span></span></code></pre>
</figure><p>Add pnetlab repository with the following command.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ <span class="nb">echo</span> <span class="s2">&#34;deb [trusted=yes] http://repo.pnetlab.com ./&#34;</span> <span class="p">|</span> sudo tee -a /etc/apt/sources.list</span></span></code></pre>
</figure><p>Update the repository and install pnetlab.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo apt-get update
</span></span><span class="line"><span class="cl">$ sudo apt-get install pnetlab</span></span></code></pre>
</figure><p>During installation process, you might be asked to replace kernel configuration like this</p>
<p><div class="img-container"><img src="./imgs/image-20260528204224869.png" alt="image-20260528204224869"  /></div>
</p>
<p>Just go with it and continue the installation.</p>
<p>Once it finished, you will have a name resolution/DNS issue, but that&rsquo;s fine for now.</p>
<h3 id="server-configuration">Server Configuration</h3>
<h4 id="swap">Swap</h4>
<p>Create a swap disk, allocate the size according to your need.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo fallocate -l 1G /swapfile
</span></span><span class="line"><span class="cl">$ sudo chmod <span class="m">600</span> /swapfile
</span></span><span class="line"><span class="cl">$ sudo mkswap /swapfile
</span></span><span class="line"><span class="cl">$ sudo swapon /swapfile
</span></span><span class="line"><span class="cl">$ sudo cp /etc/fstab<span class="o">{</span>,.bak<span class="o">}</span>
</span></span><span class="line"><span class="cl">$ sudo <span class="s1">&#39;/swapfile none swap sw 0 0&#39;</span> <span class="p">|</span> sudo tee -a /etc/fstab</span></span></code></pre>
</figure><h4 id="dns">DNS</h4>
<p>Edit file <code>/etc/network/interfaces</code> add line <code>dns-nameservers 8.8.8.8</code> to the configuration of interface <code>pnet0</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-toml" data-lang="toml"><span class="line"><span class="cl"><span class="p">...</span>
</span></span><span class="line"><span class="cl"><span class="c"># The primary network interface</span>
</span></span><span class="line"><span class="cl"><span class="nx">iface</span> <span class="nx">eth0</span> <span class="nx">inet</span> <span class="nx">manual</span>
</span></span><span class="line"><span class="cl"><span class="nx">auto</span> <span class="nx">pnet0</span>
</span></span><span class="line"><span class="cl"><span class="nx">iface</span> <span class="nx">pnet0</span> <span class="nx">inet</span> <span class="nx">dhcp</span>
</span></span><span class="line"><span class="cl">    <span class="nx">dns-nameservers</span> <span class="mf">8.8</span><span class="p">.</span><span class="mf">8.8</span>	
</span></span><span class="line"><span class="cl">    <span class="nx">bridge_ports</span> <span class="nx">eth0</span>
</span></span><span class="line"><span class="cl">    <span class="nx">bridge_stp</span> <span class="nx">off</span>
</span></span><span class="line"><span class="cl"><span class="p">...</span></span></span></code></pre>
</figure><p>And restart the networking service.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo service networking restart</span></span></code></pre>
</figure><h4 id="disk">Disk</h4>
<p>Time to add the additional disk we defined in the terraform resource. The purpose of this disk is to store all the pnetlab project files as well as the images later. We can preserve the lab data in this disk in case the VM need to be replaced/gone wrong, we will get into the detail later.</p>
<p>In my case, the additional disk is available at <code>/dev/sdb</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo lsblk
</span></span><span class="line"><span class="cl">NAME    MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
</span></span><span class="line"><span class="cl">loop0     7:0    <span class="m">0</span> 40.4M  <span class="m">1</span> loop /snap/snapd/20671
</span></span><span class="line"><span class="cl">loop1     7:1    <span class="m">0</span>  368M  <span class="m">1</span> loop /snap/google-cloud-cli/203
</span></span><span class="line"><span class="cl">loop2     7:2    <span class="m">0</span> 63.9M  <span class="m">1</span> loop /snap/core20/2105
</span></span><span class="line"><span class="cl">sda       8:0    <span class="m">0</span>   20G  <span class="m">0</span> disk 
</span></span><span class="line"><span class="cl">├─sda1    8:1    <span class="m">0</span> 19.9G  <span class="m">0</span> part /
</span></span><span class="line"><span class="cl">├─sda14   8:14   <span class="m">0</span>    4M  <span class="m">0</span> part 
</span></span><span class="line"><span class="cl">└─sda15   8:15   <span class="m">0</span>  106M  <span class="m">0</span> part /boot/efi
</span></span><span class="line"><span class="cl">sdb       8:16   <span class="m">0</span>  100G  <span class="m">0</span> disk <span class="c1"># External Disk</span></span></span></code></pre>
</figure><p>We&rsquo;ll create an LVM disk out of it.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo vgcreate /dev/vg01 /dev/sdb
</span></span><span class="line"><span class="cl">$ sudo lvcreate -l 100%FREE --name lv01 vg01 
</span></span><span class="line"><span class="cl">$ sudo mkfs.ext4 /dev/vg01/lv01 </span></span></code></pre>
</figure><p>Then we set a mount point for the volume we just created at <code>/mnt/unetlab</code></p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo mkdir /mnt/unetlab
</span></span><span class="line"><span class="cl">$ sudo mount /dev/vg01/lv01 /mnt/unetlab</span></span></code></pre>
</figure><p>Edit the <code>/etc/fstab</code> file and add the following line to mount <code>/mnt/unetlab</code> on boot:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-ini" data-lang="ini"><span class="line"><span class="cl"><span class="na">/dev/vg01/lv01  /mnt/unetlab  ext4  defaults  0  0</span></span></span></code></pre>
</figure><p>Now we&rsquo;ll create a new directory for PNETLab project files and image files in our newly mounted disk.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo mkdir /mnt/unetlab/addons
</span></span><span class="line"><span class="cl">$ sudo mkdir /mnt/unetlab/labs</span></span></code></pre>
</figure><p>Next, we&rsquo;ll remove the old directories for project files and image files, and replace them with symbolic links pointing to the new directories on the mounted disk.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo rm -rf /opt/unetlab/addons/ 
</span></span><span class="line"><span class="cl">$ sudo rm  -rf /opt/unetlab/labs/
</span></span><span class="line"><span class="cl">$ sudo ln -s /mnt/unetlab/addons/ /opt/unetlab/
</span></span><span class="line"><span class="cl">$ sudo ln -s /mnt/unetlab/labs/ /opt/unetlab/</span></span></code></pre>
</figure><p>Change ownership of the new labs directory to ensure PNETLab&rsquo;s services can access it back.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo chown -R www-data:www-data /mnt/unetlab/labs/</span></span></code></pre>
</figure><h4 id="grub">Grub</h4>
<p>Remove the default grub config.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo rm /etc/default/grub.d/50-cloudimg-settings.cfg
</span></span><span class="line"><span class="cl">$ sudo update-grub</span></span></code></pre>
</figure><h4 id="restart-server">Restart Server</h4>
<p>To complete the installation, restart the server.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo reboot</span></span></code></pre>
</figure><p>Once you logged in, there will be a pop up asking for setting root password, hostname, and NTP. Configure these according to your preferences.</p>
<p>We also need to make sure that the internet connectivity is working with simple <code>ping</code> check to <code>1.1.1.1</code>.</p>
<h3 id="verify-installation">Verify Installation</h3>
<p>Since the server configured without a public IP address, the PNETLab web interface cannot be accessed directly from the internet. The installation can be validated locally using <code>curl</code> from the server.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ curl -k https://localhost</span></span></code></pre>
</figure><p>Expected output:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">HTTP/1.1 <span class="m">301</span> Moved Permanently
</span></span><span class="line"><span class="cl">Date: Thu, <span class="m">28</span> May <span class="m">2026</span> 15:02:55 GMT
</span></span><span class="line"><span class="cl">Server: Apache/2.4.29 <span class="o">(</span>Ubuntu<span class="o">)</span>
</span></span><span class="line"><span class="cl">Access-Control-Allow-Origin: *
</span></span><span class="line"><span class="cl">Access-Control-Allow-Headers: *
</span></span><span class="line"><span class="cl">Location: /store/public/admin/main/view
</span></span><span class="line"><span class="cl">...</span></span></code></pre>
</figure><h3 id="update-version-optional">Update Version (Optional)</h3>
<p>After the installation completed, we can directly upgrade PNETLab to the latest version. It&rsquo;s an optional, so I will just put the official guide link on how to upgrade the lab version: <a href="https://pnetlab.com/pages/releases"target="_blank" rel="noopener noreferrer"
>go here</a>.</p>
<h2 id="troubleshoot">Troubleshoot</h2>
<p>I know things that may go wrong before you finish it, so there are a few problems I encountered and the solutions.</p>
<h3 id="cannot-install-packages">Cannot install packages</h3>
<p>Try disable/comment this whole url line from the repository.</p>
<figure class="highlight">
    <pre tabindex="0"><code class="language-" data-lang="">#deb [trusted=yes] http://i-share.top/repo ./
#deb [trusted=yes] http://repo.pnetlab.com ./</code></pre>
</figure><h3 id="cloud-appliance-dhcp-not-working">Cloud Appliance: DHCP not working</h3>
<p>Verify <code>udhcpd</code> service is running and enabled.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ service udhcpd status
</span></span><span class="line"><span class="cl">$ cat /etc/default/udhcpd <span class="p">|</span> grep ENABLED</span></span></code></pre>
</figure><p>Set to enabled and restart to see if it&rsquo;s working.</p>
<h2 id="conclusion">Conclusion</h2>
<p>We have completed the deployment process of PNETLab server on Google Cloud using Terraform. At this stage, the lab is running internally but remains inaccessible from outside the private network.</p>
<p>In the next article, we will configure Tailscale to provide secure remote access to the lab.</p>
]]></content:encoded>
    </item>
    
    <item>
      <title>Building Virtual Home Lab for Pentesting</title>
      <link>https://fahmifj.github.io/blog/building-virtual-home-lab-for-pentest/</link>
      <pubDate>Thu, 17 Jun 2021 14:04:24 +0700</pubDate>
      
      <guid>https://fahmifj.github.io/blog/building-virtual-home-lab-for-pentest/</guid>
      <description>Virtual home lab for offensive security practice, with 8 gigs!</description>
      <content:encoded><![CDATA[<p>This article documents a virtual Active Directory lab I built to learn some Active Directory attacks, network pivoting, and basic Command and Control (<code>C2</code>) using Metasploit.</p>
<p>The lab is designed to resemble a small enterprise Windows environment with minimal requirements that enough to run on a mid-range specification.</p>
<h2 id="assumptions--threat-model">Assumptions &amp; Threat Model</h2>
<p>This lab assumes:</p>
<ul>
<li>An internal attacker with network access</li>
<li>Minimal internal monitoring</li>
<li>Default or weak Windows security configurations</li>
<li>Misconfigured behavior</li>
</ul>
<h2 id="prerequisites">Prerequisites</h2>
<h3 id="knowledge">Knowledge</h3>
<ul>
<li>Virtualization (VirtualBox)</li>
<li>Windows and Windows Server installation</li>
<li>Basic Active Directory concepts (Domain, DNS, SPNs)</li>
<li>Basic networking and routing</li>
</ul>
<h3 id="hardware">Hardware</h3>
<p>Recommended minimum (sorted by priority):</p>
<ul>
<li><strong>Storage</strong>: 256 GB SSD (or high-speed USB 3.x)</li>
<li><strong>RAM</strong>: 8 GB minimum, 16 GB recommended</li>
<li><strong>CPU</strong>:
<ul>
<li>Minimum: Intel i3 6th gen / Ryzen 3</li>
<li>Recommended: i5 / Ryzen 5 (H or K variants)</li>
</ul>
</li>
</ul>
<blockquote>
<p>The lab was built and tested on an 8 GB system by aggressively disabling unused services after installation.</p>
</blockquote>
<h3 id="software">Software</h3>
<ul>
<li>VirtualBox (<a href="https://www.virtualbox.org/wiki/Downloads"target="_blank" rel="noopener noreferrer"
>Download</a>)</li>
<li>Kali Linux (attacker)  (<a href="https://www.offensive-security.com/kali-linux-vm-vmware-virtualbox-image-download/#1572305786534-030ce714-cc3b"target="_blank" rel="noopener noreferrer"
>Download</a>)</li>
<li>Windows 10 evaluation image file (<a href="https://www.microsoft.com/en-us/evalcenter/"target="_blank" rel="noopener noreferrer"
>Download</a>)</li>
<li>Windows Server 2019 evaluation image file (<a href="https://www.microsoft.com/en-us/evalcenter/"target="_blank" rel="noopener noreferrer"
>Download</a>)</li>
</ul>
<h2 id="topology-overview">Topology Overview</h2>
<p>The environment consists of:</p>
<ul>
<li>1 Domain Controller</li>
<li>2 Domain-joined Windows clients</li>
<li>1 Attacker machine (Kali Linux)</li>
</ul>
<p>Two network segments are used:</p>
<ul>
<li><code>192.168.1.0/24</code> – Internal domain network</li>
<li><code>10.10.10.96/28</code> – Segmented network used for pivoting scenarios</li>
</ul>
<p><div class="img-container"><img src="imgs/topology.jpg" alt=""  /></div>
</p>
<blockquote>
<p>Note: It&rsquo;s NIC (Network Interface Card) not NC</p>
</blockquote>
<h2 id="network-segments">Network Segments</h2>
<p>This lab intentionally uses two network segments to simulate an internal enterprise environment and to enable <strong>pivoting scenarios</strong> later on.</p>
<table>
<thead>
<tr>
<th>Segment</th>
<th>CIDR</th>
<th>Name</th>
<th>Purpose</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td><code>192.168.1.0/24</code></td>
<td>windows_domain</td>
<td>Main internal network (initial setup &amp; attacker access)</td>
</tr>
<tr>
<td>2</td>
<td><code>10.10.10.96/28</code></td>
<td>internal_windows</td>
<td>Restricted internal segment (pivoting target)</td>
</tr>
</tbody>
</table>
<h2 id="virtualbox-setup">VirtualBox Setup</h2>
<h3 id="system-configuration">System Configuration</h3>
<p>Initial installation:</p>
<ul>
<li>Server: <code>2424 MB</code></li>
<li>Clients: <code>1280 MB</code> each</li>
</ul>
<p>Post-installation (after disabling unnecessary services):</p>
<ul>
<li>Server: <code>1280 MB</code></li>
<li>Clients: <code>1024 MB</code></li>
<li>Attacker: <code>1024 MB</code></li>
</ul>
<p>This configuration allows all VMs to run concurrently on an 8 GB host. For initial setup, the two clients can stay inside <code>192.168.1.0/24</code> network.</p>
<h3 id="network-configuration">Network Configuration</h3>
<p>For each VM (initial setup):</p>
<ul>
<li>Adapter 1
<ul>
<li>Type: <code>Internal Network</code></li>
<li>Name: <code>windows_domain</code></li>
<li>IP Range: <code>192.168.1.0/24</code></li>
</ul>
</li>
</ul>
<p>For the Domain Controller only:</p>
<ul>
<li>Adapter 2
<ul>
<li>Name: <code>internal_windows</code></li>
<li>IP Range: <code>10.10.10.96/28</code></li>
</ul>
</li>
</ul>
<p>This makes the Domain Controller act as a bridge between segments.</p>
<p><div class="img-container"><img src="imgs/image-20210617143401181.png" alt="image-20210617143401181"  /></div>
</p>
<h2 id="active-directory-setup">Active Directory Setup</h2>
<h3 id="server">Server</h3>
<h4 id="initial-setup">Initial Setup</h4>
<ul>
<li>Admin credentials: <code>administrator:p@$$w0rd!</code></li>
<li>PC Name: <code>server19-DC</code> (restart after)</li>
<li>Network (Static):
<ul>
<li>Adapter 1: <code>192.168.1.100/24</code></li>
<li>Adapter 2: <code>10.10.10.100/28</code></li>
</ul>
</li>
</ul>
<h4 id="promote-server-to-domain-controller">Promote Server to Domain Controller</h4>
<ul>
<li>Server Manager &gt; Manage &gt; Add Roles and Features.</li>
<li>Add Roles and Features Wizard:
<ul>
<li>Installation type: &ldquo;<strong>Role-based or feature-based installation</strong>&rdquo;</li>
<li>Server selection: <code>server19-DC</code></li>
<li>Server roles: <strong>&ldquo;Active Directory Domain Services&rdquo;</strong> and check the <strong>&ldquo;Include management tools&rdquo;</strong>.</li>
<li>Features: Check the <strong>&ldquo;Group Policy Management&rdquo;</strong></li>
<li>Confirmation:  Check on <strong>&ldquo;Restart destination server automatically if required&rdquo;</strong></li>
<li>Close after it&rsquo;s done.</li>
</ul>
</li>
<li>Server Manager &gt; Notification flag &gt; Click on <strong>&ldquo;Promote this server to a domain controller&rdquo;</strong></li>
<li>Active Directory Domain Services Configuration Wizard:
<ul>
<li>Deployment configuration: <strong>&ldquo;Add a new forest&rdquo;</strong> and set <strong>&ldquo;server19.local&rdquo;</strong> as root domain name</li>
<li>Domain controller options: set <strong>&ldquo;Windows Server 2016&rdquo;</strong> as FFL (Forest Functional Level) and DFL (Domain Functional Level). Checklist DNS server and set the same admin password for DSRM password.</li>
<li>Additional options: set NetBIOS domain name to <code>SERVER19</code></li>
<li>Let the rest options in default state until installation section.</li>
<li>Restart after installation complete.</li>
</ul>
</li>
</ul>
<p><div class="img-container"><img src="imgs/server19logon.jpg" alt=""  /></div>
</p>
<h4 id="create-domain-accounts">Create Domain Accounts</h4>
<ul>
<li>John Smith
<ul>
<li>User logon name: <code>jsmith@server19.local</code></li>
<li>Password: <code>jsmith@123</code></li>
</ul>
</li>
<li>Carl Smith
<ul>
<li>User logon name: <code>cmisth@server19.local</code></li>
<li>Password: <code>@csmith@</code></li>
</ul>
</li>
</ul>
<p>All password is set to never expires.</p>
<h4 id="create-fake-service-account">Create Fake Service Account</h4>
<p>Fake SQL Service</p>
<ul>
<li>User logon name: <code>SQLService@server19.local</code></li>
<li>Password: <code>Mysql@Password123</code></li>
</ul>
<p>Set service principle name:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">setspn -a SERVER19-DC/SQLService.SERVER19.local:60111 SERVER19\SQLService
</span></span><span class="line"><span class="cl">setspn -T SERVER19.local -Q */*</span></span></code></pre>
</figure><h4 id="configure-file-sharing-smb">Configure File Sharing (SMB):</h4>
<ul>
<li>Server manager &gt; File and Storage Services &gt; Shares &gt; Task &gt; New Share.</li>
<li>New Share Wizard:
<ul>
<li>Profile: SMB Share Quick</li>
<li>Share Location: <code>C:\Shares\DATA</code> (Create the shares folder in C:)</li>
<li>Other Settings: Allow caching of share</li>
<li>Permission: Leave it default</li>
<li>Confirmation and create.</li>
</ul>
</li>
</ul>
<h3 id="clients">Clients</h3>
<h4 id="initial-setup-1">Initial Setup</h4>
<ul>
<li>Client 1:
<ul>
<li>IP: <code>192.168.1.101/24</code> (static)</li>
<li>PC name: NESCOFFEE</li>
</ul>
</li>
<li>Client 2:
<ul>
<li>IP: <code>192.168.1.102/24</code> (static)</li>
<li>PC name: MILO</li>
</ul>
</li>
</ul>
<p>For pivoting</p>
<ul>
<li>Client 2:
<ul>
<li>IP: <code>10.10.10.101/28</code>(static)</li>
</ul>
</li>
</ul>
<h4 id="create-local-accounts">Create Local Accounts</h4>
<p>Same with domain accounts, but add an <code>L</code> at the end of username/password.</p>
<ul>
<li>Username: <code>cmisthL</code>, password: <code>jsmithL@123</code></li>
<li>Username: <code>jsmithL</code>, password: <code>@csmith@</code></li>
</ul>
<h4 id="joining-domain">Joining Domain</h4>
<p>Client 1:</p>
<ul>
<li>Use Server&rsquo;s IP as DNS server: <code>192.168.1.100</code></li>
<li>Hit <code>Win+I</code>, type &ldquo;access&rdquo;, click on <strong>Connect</strong>.</li>
<li>Microsoft account window:
<ul>
<li>Click on <strong>&ldquo;Join this device to a local Active Directory domain&rdquo;</strong> under the alternate actions.</li>
<li>Use the server administrator password to join.</li>
<li>Skip the <strong>Add an account</strong> section</li>
<li>Restart</li>
</ul>
</li>
</ul>
<p>Client 2 has the same steps</p>
<h4 id="create-local-admin">Create Local Admin</h4>
<ul>
<li>Set John Smith (<code>jsmith@server19.local</code>) as local administrator for NESCOFFEE.</li>
<li>Set Carl Smith (<code>cmisth@server19.local</code>) as local administrator for MILO.</li>
</ul>
<h3 id="attacker">Attacker</h3>
<h4 id="initial-setup-2">Initial Setup</h4>
<ul>
<li>Put it on the same network</li>
<li>Set static IP: <code>192.168.1.10/24</code></li>
<li>Perform ping test</li>
</ul>
<h2 id="attack-scenarios">Attack Scenarios</h2>
<p>Here are some attack scenarios that can be reproduced using this lab:</p>
<ul>
<li>
<p>LLMNR Poisoning</p>
<ul>
<li>Example: <a href="https://www.aptive.co.uk/blog/llmnr-nbt-ns-spoofing/"target="_blank" rel="noopener noreferrer"
>https://www.aptive.co.uk/blog/llmnr-nbt-ns-spoofing/</a></li>
</ul>
</li>
<li>
<p>AS-REP Roasting</p>
<ul>
<li>Example: <a href="/tags/asrep-roasting"target="_blank" rel="noopener noreferrer"
>ASREP-Roasting tags</a></li>
</ul>
</li>
<li>
<p>Kerberoasting</p>
<ul>
<li>Example: <a href="https://pentestlab.blog/2018/06/12/kerberoast/"target="_blank" rel="noopener noreferrer"
>https://pentestlab.blog/2018/06/12/kerberoast/</a></li>
</ul>
</li>
<li>
<p>Take Over IPv6 DNS</p>
<ul>
<li>Example:  <a href="https://blog.fox-it.com/2018/01/11/mitm6-compromising-ipv4-networks-via-ipv6/"target="_blank" rel="noopener noreferrer"
>https://blog.fox-it.com/2018/01/11/mitm6-compromising-ipv4-networks-via-ipv6/</a></li>
</ul>
</li>
<li>
<p>DCSync</p>
<ul>
<li>Example: <a href="/tags/dcsync/"target="_blank" rel="noopener noreferrer"
>DCSync tags</a></li>
</ul>
</li>
</ul>
<p>Attack scenario(s) that requires two clients online + server:</p>
<ul>
<li>SMB Relay
<ul>
<li>Examples: <a href="/writeups/hackthebox/htb-apt/"target="_blank" rel="noopener noreferrer"
>HackTheBox - APT</a>, <a href="https://akimboviper.gitbook.io/pentest-everything/everything/everything-windows/attacking-windows/relay-attacks/smb-relay"target="_blank" rel="noopener noreferrer"
>SMB Relay Attack</a></li>
</ul>
</li>
</ul>
]]></content:encoded>
    </item>
    
    <item>
      <title>Exposing an SMB Server to the Internet in Azure</title>
      <link>https://fahmifj.github.io/blog/exposing-smb-server-to-internet-in-azure/</link>
      <pubDate>Sun, 04 Jul 2021 19:45:21 +0700</pubDate>
      
      <guid>https://fahmifj.github.io/blog/exposing-smb-server-to-internet-in-azure/</guid>
      <description>And wait for bad things to happen</description>
      <content:encoded><![CDATA[<p>Last month, I was asked to set up a Samba/SMB server that could be accessed over the Internet. Since the requirement was temporary, I chose to deploy it on an Azure Virtual Machine using my student subscription instead of renting a dedicated VPS.</p>
<p>Although the server ended up not being used, this article documents the setup process during the deployment.</p>
<p>Before going further, I have to say that exposing the SMB Server directly to the Internet is not considered &ldquo;best practice&rdquo;. A safer approach would be to place the server behind a VPN and only allow access through that private network.</p>
<h4 id="goals">Goals</h4>
<ul>
<li>Deploy an Ubuntu server in Azure</li>
<li>Set up and Configure a Samba server</li>
<li>Expose the Samba server over the Internet using IP whitelisting</li>
</ul>
<h4 id="prerequisites">Prerequisites</h4>
<ul>
<li>Azure Account</li>
<li>Basic knowledge of Azure, at least menu navigation and creating a resource group.</li>
<li>Basic knowledge of Linux</li>
</ul>
<h2 id="vm-configuration--deployment">VM Configuration &amp; Deployment</h2>
<p>The VM setup choices here made to support a small number of temporary users as well as keeping costs low.</p>
<h3 id="create-vm">Create VM</h3>
<p>At this step, I already have a resource group called <code>IAMF_SMB-TEST</code> and I&rsquo;ll be creating a VM instance inside this resource. I used a B1s instance since the server was intended for 4–5 concurrent users and only basic file sharing workloads.</p>
<p>The VM details of mine can be seen in the following image:</p>
<img src="imgs/image-20210513170545346.png" alt="image-20210513170545346" style="zoom: 50%;" />
<p>For authentication to the server, I&rsquo;ll be using an SSH public key here instead of a password. The account for server administration is called <code>azure-smb</code>. Since the authentication is SSH, this VM will have an SSH port open publicly (internet).</p>
<img src="imgs/image-20210513170638603.png" alt="image-20210513170638603" style="zoom:50%;" />
<p>In the following section, I&rsquo;ll use a standard SSD and leave the other settings at their defaults.</p>
<img src="imgs/image-20210513170710958.png" alt="image-20210513170710958" style="zoom:50%;" />
<p>In the <strong>Networking</strong> section, I&rsquo;ll just create a new virtual network. See the following image for details:</p>
<img src="imgs/image-20210513170824172.png" alt="image-20210513170824172" style="zoom:50%;" />
<p>In the <strong>Management</strong> section, I&rsquo;ll just disable the boot diagnostics.</p>
<img src="imgs/image-20210513171009570.png" alt="image-20210513171009570" style="zoom:50%;" />
<p>I&rsquo;ll leave the <strong>Advanced</strong> with the default settings and skip the <strong>Tags</strong> section.</p>
<p>The last section is <strong>Review + Create</strong>, which basically a section to review the VM configuration. After I finish the review, I&rsquo;ll press the <strong>Create</strong> button.</p>
<img src="imgs/image-20210513171153896.png" alt="image-20210513171153896" style="zoom:50%;" />
<p>When the <strong>Create</strong> button is clicked, the VM will be deployed automatically.</p>
<img src="imgs/image-20210513171501031.png" alt="image-20210513171501031" style="zoom:50%;" />
<h3 id="access-vm">Access VM</h3>
<p>Once the machine or VM has been deployed. Log into it using SSH. The public IP of the VM instance can be found at the <code>Dashboard</code> &gt; <code>RESOURCE_GROUP_NAME</code> &gt; <code>PUBLIC_IP_NAME</code>. In my case, it is <code>Dashboard</code> &gt; <code>IAMF_SMB-TEST</code> &gt; <code>smb-server-ip</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ ssh -i private_key azure-smb@PUBLIC_IP_ADDRESS</span></span></code></pre>
</figure><img src="imgs/image-20210513171606268.png" alt="image-20210513171606268" style="zoom:50%;" />
<h2 id="samba">Samba</h2>
<h3 id="repo-update">Repo Update</h3>
<p>First thing first, let&rsquo;s update the repository list.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo apt update</span></span></code></pre>
</figure><h3 id="install-samba">Install Samba</h3>
<p>After that, install <code>Samba</code> with the following command.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo apt install samba</span></span></code></pre>
</figure><p>Once the installation is done, check the Samba service daemon status.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo systemctl status smbd
</span></span><span class="line"><span class="cl">● smbd.service - Samba SMB Daemon
</span></span><span class="line"><span class="cl">   Loaded: loaded <span class="o">(</span>/lib/systemd/system/smbd.service<span class="p">;</span> enabled<span class="p">;</span> vendor preset: enabled<span class="o">)</span>
</span></span><span class="line"><span class="cl">   Active: active <span class="o">(</span>running<span class="o">)</span> since Thu 2021-05-13 10:17:45 UTC<span class="p">;</span> 3min 48s ago
</span></span><span class="line"><span class="cl">     Docs: man:smbd<span class="o">(</span>8<span class="o">)</span>
</span></span><span class="line"><span class="cl">           man:samba<span class="o">(</span>7<span class="o">)</span>
</span></span><span class="line"><span class="cl">           man:smb.conf<span class="o">(</span>5<span class="o">)</span>
</span></span><span class="line"><span class="cl"> Main PID: <span class="m">2098</span> <span class="o">(</span>smbd<span class="o">)</span>
</span></span><span class="line"><span class="cl">   Status: <span class="s2">&#34;smbd: ready to serve connections...&#34;</span>
</span></span><span class="line"><span class="cl">    Tasks: <span class="m">4</span> <span class="o">(</span>limit: 1056<span class="o">)</span>
</span></span><span class="line"><span class="cl">   CGroup: /system.slice/smbd.service
</span></span><span class="line"><span class="cl">           ├─2098 /usr/sbin/smbd --foreground --no-process-group
</span></span><span class="line"><span class="cl">           ├─2123 /usr/sbin/smbd --foreground --no-process-group
</span></span><span class="line"><span class="cl">           ├─2124 /usr/sbin/smbd --foreground --no-process-group
</span></span><span class="line"><span class="cl">           └─2129 /usr/sbin/smbd --foreground --no-process-group
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">May <span class="m">13</span> 10:17:44 smb-server systemd<span class="o">[</span>1<span class="o">]</span>: Starting Samba SMB Daemon...
</span></span><span class="line"><span class="cl">May <span class="m">13</span> 10:17:45 smb-server systemd<span class="o">[</span>1<span class="o">]</span>: Started Samba SMB Daemon.</span></span></code></pre>
</figure><p>Samba is ready, and now let&rsquo;s configure the share folder.</p>
<h3 id="configure-shares">Configure Shares</h3>
<p>First, let&rsquo;s create a backup file of the original configuration, so we can reset it to the default configuration, just in case something goes wrong.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo cp /etc/samba/smb.conf<span class="o">{</span>,.backup<span class="o">}</span></span></span></code></pre>
</figure><p>Now create a share folder name it <code>sambashare</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ mkdir sambashare</span></span></code></pre>
</figure><p>Then open the samba configuration file with a text editor like <code>nano</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo nano /etc/samba/smb.conf</span></span></code></pre>
</figure><p>Go straight to the bottom of the file and add the following lines.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl"><span class="o">[</span>sambashare<span class="o">]</span>
</span></span><span class="line"><span class="cl">    <span class="nv">comment</span> <span class="o">=</span> Samba Share
</span></span><span class="line"><span class="cl">    <span class="nv">path</span> <span class="o">=</span> /home/azure-smb/sambashare
</span></span><span class="line"><span class="cl">    <span class="nb">read</span> <span class="nv">only</span> <span class="o">=</span> no
</span></span><span class="line"><span class="cl">    <span class="nv">browsable</span> <span class="o">=</span> yes</span></span></code></pre>
</figure><blockquote>
<p>Note: Details for configuring a share can be read <a href="https://www.samba.org/samba/docs/current/man-html/smb.conf.5.html"target="_blank" rel="noopener noreferrer"
>here</a> or <a href="https://web.mit.edu/rhel-doc/5/RHEL-5-manual/Deployment_Guide-en-US/s1-samba-configuring.html"target="_blank" rel="noopener noreferrer"
>here</a>.</p>
</blockquote>
<p>Save the file and restart the SMB daemon with the following command:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo service smbd restart</span></span></code></pre>
</figure><p>Lastly, update the firewall to allow network traffic for Samba/SMB.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo ufw allow samba</span></span></code></pre>
</figure><p>This is the basic configuration of creating a Samba share, but from here you can create another share with more complex configuration. Here are my references:</p>
<ul>
<li><a href="https://linuxize.com/post/how-to-install-and-configure-samba-on-ubuntu-18-04/"target="_blank" rel="noopener noreferrer"
>https://linuxize.com/post/how-to-install-and-configure-samba-on-ubuntu-18-04/</a></li>
<li><a href="https://confluence.jaytaala.com/display/TKB/Create&#43;samba&#43;share&#43;writeable&#43;by&#43;all%2C&#43;group%2C&#43;or&#43;only&#43;a&#43;user"target="_blank" rel="noopener noreferrer"
>https://confluence.jaytaala.com/display/TKB/Create&#43;samba&#43;share&#43;writeable&#43;by&#43;all%2C&#43;group%2C&#43;or&#43;only&#43;a&#43;user</a></li>
<li><a href="https://www.digitalocean.com/community/tutorials/how-to-set-up-a-samba-share-for-a-small-organization-on-ubuntu-16-04"target="_blank" rel="noopener noreferrer"
>https://www.digitalocean.com/community/tutorials/how-to-set-up-a-samba-share-for-a-small-organization-on-ubuntu-16-04</a></li>
</ul>
<h3 id="add-samba-user">Add Samba User</h3>
<p>Currently, our Linux account for administering the server is <code>azure-smb</code> and we shouldn&rsquo;t use this account password to access the SMB shares. So, let&rsquo;s just create a dedicated user for SMB called <code>user1</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo useradd --system -s /usr/sbin/nologin user1</span></span></code></pre>
</figure><p>Assign <code>user1</code> to be the owner of the share</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo chown user1 /home/azure-smb/sambashare</span></span></code></pre>
</figure><p>After that, create a Samba password for <code>user1</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo smbpasswd -a user1
</span></span><span class="line"><span class="cl">New SMB password: 
</span></span><span class="line"><span class="cl">Retype new SMB password: 
</span></span><span class="line"><span class="cl">Added user user1.</span></span></code></pre>
</figure><p>Finally, enable the user.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">azure-smb@smb-server:~$ sudo smbpasswd -e user1
</span></span><span class="line"><span class="cl">Enabled user user1.</span></span></code></pre>
</figure><h2 id="expose-smb-to-the-internet">Expose SMB to the Internet</h2>
<h3 id="allow-inbound-connection">Allow Inbound Connection</h3>
<p>Now if we want to make the SMB server available on the Internet, we have to go back to the Azure Portal to open the SMB port (port 445) on the NIC Public IP and allow inbound connection through that port. The connection is then forwarded to our SMB port on the NIC Private IP.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">PUBLIC_IP:445 --&gt; PRIVATE_IP:445</span></span></code></pre>
</figure><p>To do that open up the Networking settings of the SMB VM and click on <strong>Add inbound port rule</strong> button.</p>
<img src="imgs/image-20210513173838694.png" alt="image-20210513173838694" style="zoom:50%;" />
<p>On the new Windows, configure the rule to allow any source (incoming IP) for <strong>non-whitelist</strong> or <strong>input the whitelisted IP</strong> in the Source section. Then, for source port ranges (incoming port) let it be asterisk (<code>*</code>) to allow any port to connect to our SMB services(port <code>445</code>).</p>
<p>The configuration is as follows:</p>
<img src="imgs/image-20210513173858659.png" alt="image-20210513173858659" style="zoom:50%;" />
<p>At the bottom, the configuration is as follows:</p>
<img src="imgs/image-20210513173929271.png" alt="image-20210513173929271" style="zoom:50%;" />
<p>Once done, click on the <strong>Add</strong> button and the new rule should listed in the <strong>Inbound port rules</strong> section.</p>
<img src="imgs/image-20210513174018895.png" alt="image-20210513174018895" style="zoom:50%;" />
<h3 id="verify-public-access">Verify Public Access</h3>
<p>We can use <code>nmap</code> to see if the SMB port has been opened.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ nmap -p445 -sV VM_PUBLIC_IP</span></span></code></pre>
</figure><img src="imgs/image-20210513174227862.png" alt="image-20210513174227862" style="zoom:50%;" />
<p>To interact with the SMB server via CLI, you can use <code>smbclient</code>. Install it with:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo apt install smbclient</span></span></code></pre>
</figure><p>Once it installed, connect to the share with following command:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ smbclient //<span class="o">[</span>IP<span class="o">]</span>/<span class="o">[</span>sharename<span class="o">]</span> </span></span></code></pre>
</figure><p>Adding <code>-N -L</code> can list all the available shares.</p>
<img src="imgs/image-20210513174416250.png" alt="image-20210513174416250" style="zoom:50%;" />
<p>You can also provide the password directly in the terminal:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ smbclient //<span class="o">[</span>IP<span class="o">]</span>/<span class="o">[</span>sharename<span class="o">]</span> -U <span class="o">[</span>username<span class="o">]</span> <span class="o">[</span>password<span class="o">]</span>
</span></span><span class="line"><span class="cl">$ smbclient //<span class="o">[</span>IP<span class="o">]</span>/<span class="o">[</span>sharename<span class="o">]</span> -U <span class="s1">&#39;username%password&#39;</span></span></span></code></pre>
</figure><p>This setup worked as expected for the original requirement, although it was not used. If I were to implement this again, I would place the SMB server behind a VPN and avoid exposing port 445 directly to the Internet.</p>
]]></content:encoded>
    </item>
    
    <item>
      <title>PNETLab on Google Cloud - Part 2: Remote Access with Tailscale</title>
      <link>https://fahmifj.github.io/blog/deploy-pnetlab-on-gcp-part-2-remote-access-tailscale/</link>
      <pubDate>Tue, 09 Jun 2026 17:08:00 +0700</pubDate>
      
      <guid>https://fahmifj.github.io/blog/deploy-pnetlab-on-gcp-part-2-remote-access-tailscale/</guid>
      <description>Introduction In the first part, we went through the deployment process of PNETLab on Google Cloud Platform using Terraform. The server was intentionally configured without Public IP address, preventing it accessible from the internet.
With this approach, we still need a way to access it securely. Normally, we would deploy a VPN server and configure it manually. However, we will use a zero configuration VPN solution from Tailscale.
This articles covers the Tailscale setup, advertise the appliance network to the Tailnet, and sharing the lab with other users within the Tailnet with least privilege principle.</description>
      <content:encoded><![CDATA[<h2 id="introduction">Introduction</h2>
<p>In the <a href="/articles/deploy-pnetlab-on-gcp-part-1/"target="_blank" rel="noopener noreferrer"
>first part</a>, we went through the deployment process of PNETLab on Google Cloud Platform using Terraform. The server was intentionally configured without Public IP address, preventing it accessible from the internet.</p>
<p>With this approach, we still need a way to access it securely. Normally, we would deploy a VPN server and configure it manually. However, we will use a zero configuration VPN solution from Tailscale.</p>
<p>This articles covers the Tailscale setup, advertise the appliance network to the Tailnet, and sharing the lab with other users within the Tailnet with least privilege principle. We will also configure a custom domain on the lab web interface.</p>
<h3 id="goals">Goals</h3>
<ul>
<li>Secure access to the lab without exposing it to the public internet.</li>
<li>Configure a domain name for easier access to the lab.</li>
</ul>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>Tailscale accounts.</li>
<li>Own a custom domain name.</li>
</ul>
<h2 id="architecture-overview">Architecture Overview</h2>
<img src="./imgs/image-20251028001404879.png" alt="image-20251028001404879" style="zoom:50%;" />
<p>The setup consists of:</p>
<ul>
<li>Terraform for infrastructure automation</li>
<li>A GCP compute instance running PNETLab</li>
<li>A private VPC network</li>
<li>Cloud Router for outbound internet access (dynamic IP)</li>
<li><strong>Tailscale for secure remote access to PNETLab instance</strong></li>
</ul>
<h2 id="tailscale-installation">Tailscale Installation</h2>
<p>To use the Tailscale VPN service, we need an agent like software to be installed in our server and your home devices that you are going to use to access the lab.</p>
<h3 id="install-tailscale">Install Tailscale</h3>
<p>From the pnetlab server, install Tailscale with:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo curl -fsSL https://tailscale.com/install.sh <span class="p">|</span> sh </span></span></code></pre>
</figure><p>Verify installation</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo systemctl status tailscaled <span class="p">|</span> grep active</span></span></code></pre>
</figure><p>Expected outputs:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">Active: active (running) since Thu 2026-05-28 15:54:08 UTC; 3min 25s ago</span></span></code></pre>
</figure><h2 id="joining-tailscale-network">Joining Tailscale Network</h2>
<p>We will add the server to our Tailnet and get a private IP address assigned to it. You will also need to join your home devices to Tailnet!</p>
<h3 id="add-server-to-tailscale-network">Add Server to Tailscale Network</h3>
<p>From the server, login into your <a href="https://login.tailscale.com"target="_blank" rel="noopener noreferrer"
>Tailscale</a> account via CLI:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo tailscale up</span></span></code></pre>
</figure><p>Expected outputs:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">To authenticate, visit:
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">	https://login.tailscale.com/a/random-string</span></span></code></pre>
</figure><p>Open the URL in a browser and perform the authentication with your Tailscale account.</p>
<img src="./imgs/image-20260528230531568.png" alt="image-20260528230531568" style="zoom:40%;" />
<p>After completing authentication process, the server will  join the Tailnet and receive a private Tailscale IP address and to see the assigned IP address, visit <a href="https://login.tailscale.com/admin"target="_blank" rel="noopener noreferrer"
>https://login.tailscale.com/admin</a>.</p>
<p><div class="img-container"><img src="./imgs/image-20260528231028302.png" alt="image-20260528231028302"  /></div>
</p>
<h3 id="advertise-pnetlab-network">Advertise PNETLab Network</h3>
<p>By default, Tailscale only provides connectivity to the server itself. However, PNETLab appliances run within their own subnet. Therefore, to allow devices connected to the tailnet to reach those appliances directly, the PNETLab server need to be configured as a <a href="https://tailscale.com/kb/1019/subnets"target="_blank" rel="noopener noreferrer"
>subnet router</a> or simply turning the server into a <strong>router</strong>.</p>
<p>This will enable direct access between PNETLab appliances network (including docker) and our local network via the Tailscale network.</p>
<p>In this deployment, <code>10.177.0.0/16</code> is the internal network used by PNETLab for Docker containers. As an example, to advertise that subnet within the tailnet, we run the following command.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo tailscale up --advertise-route<span class="o">=</span>10.177.0.0/16</span></span></code></pre>
</figure><blockquote>
<p>Note:</p>
<ul>
<li>The <code>--advertise-routes=&quot;10.177.0.0/16&quot;</code> option tells the instance to advertise the subnet <code>10.177.0.0/16</code> to your Tailscale network. Once approved and advertised, other devices in your Tailscale network can access the whole subnet through this instance.</li>
</ul>
</blockquote>
<p>Visit your Tailscale admin panel and will see a <code>Subnets</code> label on the pnetlab server.</p>
<p><div class="img-container"><img src="./imgs/image-20260528231822588.png" alt="image-20260528231822588"  /></div>
</p>
<p>Open the node settings, select &ldquo;<strong>edit route settings&hellip;</strong>&rdquo; and tick the subnet.</p>
<img src="./imgs/image-20260528232015004.png" alt="image-20260528232015004" style="zoom: 50%;" />
<p>From the PNETLab server, we then must enable the IP forwarding.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ <span class="nb">echo</span> <span class="s1">&#39;net.ipv4.ip_forward = 1&#39;</span> <span class="p">|</span> sudo tee -a /etc/sysctl.d/99-tailscale.conf
</span></span><span class="line"><span class="cl">$ <span class="nb">echo</span> <span class="s1">&#39;net.ipv6.conf.all.forwarding = 1&#39;</span> <span class="p">|</span> sudo tee -a /etc/sysctl.d/99-tailscale.conf
</span></span><span class="line"><span class="cl">$ sudo sysctl -p /etc/sysctl.d/99-tailscale.conf</span></span></code></pre>
</figure><h3 id="verify">Verify</h3>
<p>Now verify the subnet routing is working, try pinging the appliances subnet gateway from a device connected to the Tailnet.  If the route succeeds, we should get the reply indicating the appliances subnet is reachable through Tailnet via PNETLab server.</p>
<img src="./imgs/image-20251028003923861.png" alt="image-20251028003923861" style="zoom: 33%;" />
<p>Now we can also access the PNETLab web interface either using the tailscale private IP address or the subnet you desired.</p>
<img src="./imgs/image-20260528233456866.png" alt="image-20260528233456866" style="zoom:33%;" />
<blockquote>
<p>Note: I won&rsquo;t go deep into neither the PNETLab web configuration nor appliance installation.</p>
</blockquote>
<h2 id="sharing-access-with-other-users">Sharing Access with Other Users</h2>
<p>One of the reasons of using Tailscale is I can easily share access with other users for collaboration purposes.</p>
<p>The access itself can be controlled using ACLs, allowing users to access only the PNETLab server while restricting access to other devices within the Tailnet.</p>
<h3 id="example-acls">Example ACLs</h3>
<p>In this example, User A is the Tailnet owner and User B is a collaborator. The goal is to allow User B to access the PNETLab only and lab appliances (within the advertised route). This could be simplified using the permission table below.</p>
<table>
<thead>
<tr>
<th>Resource</th>
<th>User A (Owner)</th>
<th>User B (Collaborator)</th>
</tr>
</thead>
<tbody>
<tr>
<td>PNETLab Web Interface</td>
<td>✅</td>
<td>✅</td>
</tr>
<tr>
<td>PNETLab Appliances Network (10.177.0.0/16)</td>
<td>✅</td>
<td>✅</td>
</tr>
<tr>
<td>Other Tailnet Devices</td>
<td>✅</td>
<td>❌</td>
</tr>
<tr>
<td>Tailnet Administration</td>
<td>✅</td>
<td>❌</td>
</tr>
</tbody>
</table>
<p>First, invite User B to the Tailnet from the Tailscale <a href="https://login.tailscale.com/admin/users"target="_blank" rel="noopener noreferrer"
>admin console</a>. Once the invitation is accepted, create a <code>collaborator</code> group and add User B to it from the <a href="https://login.tailscale.com/admin/acls/file"target="_blank" rel="noopener noreferrer"
>Access Control</a> section.</p>
<figure class="highlight">
    <figcaption>
        <span class="hl-filename">groups block</span>
    </figcaption>
    <pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="s2">&#34;groups&#34;</span><span class="err">:</span> 
</span></span><span class="line"><span class="cl"><span class="p">{</span> 
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;group:collaborator&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;UserB@mail.com&#34;</span><span class="p">]</span> 
</span></span><span class="line"><span class="cl"><span class="p">}</span></span></span></code></pre>
</figure><p>Next, create a  <code>lab</code> tag</p>
<figure class="highlight">
    <figcaption>
        <span class="hl-filename">tagOwners block</span>
    </figcaption>
    <pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"> <span class="s2">&#34;tagOwners&#34;</span><span class="err">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;tag:lab&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;autogroup:owner&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span></span></span></code></pre>
</figure><p>Assign the  <code>tag:lab</code> to the PNETLab node from the admin console.</p>
<img src="./imgs/image-20260607232829657.png" alt="image-20260607232829657" style="zoom: 33%;" />
<p>Then we grant members of the <code>group:collaborator</code> to access only devices tagged with <code>tag:lab</code>.</p>
<figure class="highlight">
    <figcaption>
        <span class="hl-filename">grants block</span>
    </figcaption>
    <pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl">  <span class="s2">&#34;grants&#34;</span><span class="err">:</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;src&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;group:collaborator&#34;</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;dst&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;tag:lab&#34;</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;ip&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;*&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">]</span></span></span></code></pre>
</figure><p>This ACLs rules ensure that the collaborator like user B would never reach anything outside the lab.</p>
<h2 id="configure-domain-name">Configure Domain Name</h2>
<p>This step is generally similar to configuring a custom domain for websites deployment. I will assume that you already own a custom domain and understand how to complete an ACME DNS challenge (proving control over a domain).</p>
<p>We will use SSL certificates from LetsEncrypts and then configure a new DNS record for a custom domain like: <code>pnetlab.yourdomain.com</code>. It&rsquo;s just the record will be pointing to the Tailscale private IP of our PNETLab server.</p>
<h3 id="obtain-letsencrypt-ssl">Obtain LetsEncrypt SSL</h3>
<p>First, install <code>certbot</code> and prepare the work folder.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo apt install -y certbot</span></span></code></pre>
</figure><p>Next, get LetsEncrypt certificates by running the following command.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo certbot certonly --agree-tos --email admin@yourmail.com --manual --preferred-challenges<span class="o">=</span>dns -d pnetlab.yourdomain.com</span></span></code></pre>
</figure><p>Expected results:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">Plugins selected: Authenticator manual, Installer None
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
</span></span><span class="line"><span class="cl">Would you be willing to share your email address with the Electronic Frontier
</span></span><span class="line"><span class="cl">Foundation, a founding partner of the Let&#39;s Encrypt project and the non-profit
</span></span><span class="line"><span class="cl">organization that develops Certbot? We&#39;d like to send you email about our work
</span></span><span class="line"><span class="cl">encrypting the web, EFF news, campaigns, and ways to support digital freedom.
</span></span><span class="line"><span class="cl">- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
</span></span><span class="line"><span class="cl">(Y)es/(N)o: no
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">... [SNIP] ...
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Please deploy a DNS TXT record under the name
</span></span><span class="line"><span class="cl">_acme-challenge.pnetlab.yourdomain.com with the following value:
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">dUQSpAWIqR10KEbL-VCyZzRmK9JJpM4vBd_FScBCfnI
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Before continuing, verify the record is deployed.
</span></span><span class="line"><span class="cl">- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
</span></span><span class="line"><span class="cl">Press Enter to Continue</span></span></code></pre>
</figure><p>This step requires you to complete a ACME DNS challenge on your DNS provider (NameCheap, Hostinger, etc).</p>
<figure>
    <img src="./imgs/image-20260529165147636.png"
         alt="DNS TXT record"/> <figcaption>
            <p>DNS TXT record
            </p>
        </figcaption>
</figure>

<p>After successful validation, the CA (LetsEncrypt) will issue the certificates, which should be available at <code>./letsencrypt/config/live/yourdomain.com/</code> or <code>/etc/letsencrypt/live/yourdomain.com/</code>.</p>
<h3 id="configure-a-new-dns-record">Configure a New DNS record</h3>
<p>Go to your DNS hosting provider and add a new record with the following data:</p>
<ul>
<li>Type: <code>A</code></li>
<li>Hostname/Domain Name: <code>pnetlab.yourdomain.com</code></li>
<li>IPv4: <code>[PNETLAB TAILSCALE IP]</code></li>
</ul>
<p>Here&rsquo;s an example of mine, where I&rsquo;m using Cloudflare for DNS management.</p>
<img src="./imgs/image-20250326102815535.png" alt="image-20250326102815535" style="zoom: 33%;" />
<p><code>100.126.1.2</code> is the tailscale node IP of my pnetlab server.</p>
<p>Verify if your domain is mapped correctly using the <code>dig</code> command or <a href="https://toolbox.googleapps.com/apps/dig/#A/"target="_blank" rel="noopener noreferrer"
>Google Toolbox</a>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ dig pnetlab.fahmifj.space                         
</span></span><span class="line"><span class="cl">...<span class="o">[</span>SNIP<span class="o">]</span>...
</span></span><span class="line"><span class="cl"><span class="p">;;</span> QUESTION SECTION:
</span></span><span class="line"><span class="cl"><span class="p">;</span>pnetlab.fahmifj.space.	IN	A
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">;;</span> ANSWER SECTION:
</span></span><span class="line"><span class="cl">pnetlab.fahmifj.space. 300	IN	A	100.126.1.2
</span></span><span class="line"><span class="cl">...<span class="o">[</span>SNIP<span class="o">]</span>...</span></span></code></pre>
</figure><h3 id="configure-ssl-certificates">Configure SSL Certificates</h3>
<p>Now we have obtained the SSL certificates and pointing a custom domain to the PNETLab private IP, it&rsquo;s time to configure the web server.</p>
<p>Open and edit the apache web config file: <code>/etc/apache2/sites-available/pnetlabs.conf</code>, then change these values with your certificate path.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-ini" data-lang="ini"><span class="line"><span class="cl"><span class="na">SSLCertificateFile   /etc/letsencrypt/live/pnetlab.yourdomain.com/cert.pem</span>
</span></span><span class="line"><span class="cl"><span class="na">SSLCertificateKeyFile /etc/letsencrypt/live/pnetlab.yourdomain.com/privkey.pem</span></span></span></code></pre>
</figure><p>Restart the apache services</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ sudo systemctl restart apache2</span></span></code></pre>
</figure><h3 id="verify-1">Verify</h3>
<p>With all the required steps has been completed, we can verify the domain name configuration by accessing the PNETLab web interface device connected to the tailscale network.</p>
<figure>
    <img src="./imgs/image-20260529171044814.png"
         alt="Accessing PNETLab using Domain Name"/> <figcaption>
            <p>Accessing PNETLab using Domain Name
            </p>
        </figcaption>
</figure>

<h2 id="troubleshoot">Troubleshoot</h2>
<p>If you&rsquo;re unable to install Tailscale, try removing these repo list from the apt sources:</p>
<figure class="highlight">
    <pre tabindex="0"><code class="language-" data-lang="">$ sudo vi /etc/apt/sources.list
...
#deb [trusted=yes] http://repo.pnetlab.com ./
#deb [trusted=yes] http://i-share.top/repo ./</code></pre>
</figure><h2 id="conclusion">Conclusion</h2>
<p>With Tailscale, we have secured access to the PNETLab server without the need to manage a dedicated VPN server. We only allow access to the lab from authorized devices.</p>
<p>Tailscale also makes it easy to share the lab with other users or colleagues. By enforcing ACL rules, access can be restricted to only the resources required for collaboration. This follows the least privilege principle where access is granted based on identity.</p>
]]></content:encoded>
    </item>
    
    <item>
      <title>How to Run GNS3 Server on Apple M2 Using OrbStack</title>
      <link>https://fahmifj.github.io/blog/running-gns3-on-apple-m2-orbstack/</link>
      <pubDate>Wed, 29 Jan 2025 20:36:34 +0700</pubDate>
      
      <guid>https://fahmifj.github.io/blog/running-gns3-on-apple-m2-orbstack/</guid>
      <description>Lately, I&amp;rsquo;ve been switching from my Windows laptop to MacBook M2 for daily driver. It was smooth until I needed to migrate my virtual networking lab that relies on GNS3. I was initially hesitant about whether I could run GNS3 on my M2 machine due to lack of nested virtualization. But according to this post, I found out there&amp;rsquo;s a workaround for this using OrbStack. In this post, I will provide a walkthrough on how to deploy GNS3 on OrbStack.</description>
      <content:encoded><![CDATA[<p>Lately, I&rsquo;ve been switching from my Windows laptop to MacBook M2 for daily driver. It was smooth until I needed to migrate my virtual networking lab that relies on GNS3. I was initially hesitant about whether I could run GNS3 on my M2 machine due to lack of nested virtualization. But according to this <a href="https://www.gns3.com/community/featured/gns3-on-apple-silicon"target="_blank" rel="noopener noreferrer"
>post</a>, I found out there&rsquo;s a workaround for this using OrbStack. In this post, I will provide a walkthrough on how to deploy GNS3 on OrbStack.</p>
<p>[Update]</p>
<p>I&rsquo;ve updated this guide. I will now using GNS3 <code>pipx</code> version instead of the PPA repository. The reason is it&rsquo;s more isolated and get faster update.</p>
<h4 id="goals">Goals</h4>
<ul>
<li>Get GNS3 server running on M2 Apple Silicon CPU.</li>
<li>Get GNS3 appliance installed on the GNS3 server.</li>
</ul>
<h4 id="prerequisites">Prerequisites</h4>
<ul>
<li><a href="https://orbstack.dev/"target="_blank" rel="noopener noreferrer"
>Orbstack</a> installed.</li>
<li><a href="https://github.com/gns3/gns3-gui/releases"target="_blank" rel="noopener noreferrer"
>GNS3 GUI/client</a> installed.</li>
</ul>
<h4 id="diagram">Diagram</h4>
<p>I drew a diagram to give you an overview of what we are going to do here.</p>
<img src="./imgs/image-20250129212031966.png" alt="image-20250129212031966" style="zoom: 50%;" />
<h2 id="on-mac">On Mac</h2>
<h3 id="create-a-linux-machine-on-orbstack">Create a Linux Machine on Orbstack</h3>
<p>With Orbstack, create a new Linux machine with the latest Ubuntu distro.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ orb create --arch amd64 ubuntu gns3-host</span></span></code></pre>
</figure><p>Verify the machine is running.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ orb list
</span></span><span class="line"><span class="cl">NAME       STATE    DISTRO  VERSION   ARCH
</span></span><span class="line"><span class="cl">----       -----    ------  -------   ----
</span></span><span class="line"><span class="cl">gns3-host  running  ubuntu  oracular  amd64</span></span></code></pre>
</figure><p>That&rsquo;s it. Such an easy deployment, isn&rsquo;t it? The Linux machine we just installed will act as the GNS3 server host.</p>
<h3 id="allocating-resources">Allocating Resources</h3>
<p>We can allocate CPU and memory resources to limit how much Orbstack machine can use with:</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl"><span class="c1">#Show current configuration</span>
</span></span><span class="line"><span class="cl">$ orb config show
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">#Set maximum CPU cores for Orbstack to 4 cores</span>
</span></span><span class="line"><span class="cl">$ orb config <span class="nb">set</span> cpu <span class="m">4</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">#Set maximum memory for Orbstack to 8 GB</span>
</span></span><span class="line"><span class="cl">$ orb config <span class="nb">set</span> memory_mib <span class="m">8000</span></span></span></code></pre>
</figure><h2 id="on-the-linux-machine">On the Linux machine</h2>
<h3 id="install-gns3-server">Install GNS3 Server</h3>
<p>From Mac, login into the machine.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">$ orb -m gns3-host -u root</span></span></code></pre>
</figure><p>Install the tools.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># apt update</span>
</span></span><span class="line"><span class="cl">root@gns3 <span class="c1"># apt install software-properties-common</span>
</span></span><span class="line"><span class="cl">root@gns3 <span class="c1"># apt install qemu-system-x86</span>
</span></span><span class="line"><span class="cl">root@gns3 <span class="c1"># apt install telnet</span>
</span></span><span class="line"><span class="cl">root@gns3 <span class="c1"># apt install pipx</span></span></span></code></pre>
</figure><p>Create a GNS3 user: <code>gns3-server</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># mkdir /opt/gns3</span>
</span></span><span class="line"><span class="cl">root@gns3 <span class="c1"># adduser --system --group --home /opt/gns3 gns3-server</span></span></span></code></pre>
</figure><p>In the middle way, select &ldquo;yes&rdquo; when you prompted with something like this.</p>
<img src="./imgs/image-20250129214423431.png" alt="image-20250129214423431" style="zoom:50%;" />
<p>If you missed it, run the following.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># sudo usermod -aG ubridge,libvirt gns3-server</span></span></span></code></pre>
</figure><p>Perform GNS3 server installation.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># sudo -u gns3-server pipx install gns3-server==2.2.54 --system-site-packages</span></span></span></code></pre>
</figure><p>GNS3 will be installed under <code>/opt/gns3/.local/share/pipx/venvs/gns3-server/</code>.</p>
<h3 id="configure-gns3-server">Configure GNS3 Server</h3>
<p>Since KVM is not supported, we need to make some adjustments for GNS3 server config. Edit or create a config file as follows:</p>
<figure class="highlight">
    <figcaption>
        <span class="hl-filename">gns3_server.conf</span>
    </figcaption>
    <pre tabindex="0" class="chroma"><code class="language-toml" data-lang="toml"><span class="line"><span class="cl"><span class="p">[</span><span class="nx">Server</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="nx">host</span> <span class="p">=</span> <span class="mf">0.0</span><span class="p">.</span><span class="mf">0.0</span>
</span></span><span class="line"><span class="cl"><span class="nx">port</span> <span class="p">=</span> <span class="mi">3080</span>
</span></span><span class="line"><span class="cl"><span class="nx">images_path</span> <span class="p">=</span> <span class="err">/</span><span class="nx">opt</span><span class="err">/</span><span class="nx">gns3</span><span class="err">/</span><span class="nx">images</span>
</span></span><span class="line"><span class="cl"><span class="nx">appliances_path</span> <span class="p">=</span> <span class="err">/</span><span class="nx">opt</span><span class="err">/</span><span class="nx">gns3</span><span class="err">/</span><span class="nx">appliances</span>
</span></span><span class="line"><span class="cl"><span class="nx">configs_path</span> <span class="p">=</span> <span class="err">/</span><span class="nx">opt</span><span class="err">/</span><span class="nx">gns3</span><span class="err">/</span><span class="nx">configs</span>
</span></span><span class="line"><span class="cl"><span class="nx">projects_path</span> <span class="p">=</span> <span class="err">/</span><span class="nx">opt</span><span class="err">/</span><span class="nx">gns3</span><span class="err">/</span><span class="nx">projects</span>
</span></span><span class="line"><span class="cl"><span class="nx">symbols_path</span> <span class="p">=</span> <span class="err">/</span><span class="nx">opt</span><span class="err">/</span><span class="nx">gns3</span><span class="err">/</span><span class="nx">symbols</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nx">report_errors</span> <span class="p">=</span> <span class="nx">True</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">First</span> <span class="nx">console</span> <span class="nx">port</span> <span class="nx">of</span> <span class="nx">the</span> <span class="nx">range</span> <span class="nx">allocated</span> <span class="nx">to</span> <span class="nx">devices</span>
</span></span><span class="line"><span class="cl"><span class="nx">console_start_port_range</span> <span class="p">=</span> <span class="mi">5000</span>
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">Last</span> <span class="nx">console</span> <span class="nx">port</span> <span class="nx">of</span> <span class="nx">the</span> <span class="nx">range</span> <span class="nx">allocated</span> <span class="nx">to</span> <span class="nx">devices</span>
</span></span><span class="line"><span class="cl"><span class="nx">console_end_port_range</span> <span class="p">=</span> <span class="mi">10000</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">First</span> <span class="nx">VNC</span> <span class="nx">console</span> <span class="nx">port</span> <span class="nx">of</span> <span class="nx">the</span> <span class="nx">range</span> <span class="nx">allocated</span> <span class="nx">to</span> <span class="nx">devices</span><span class="p">.</span>
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">The</span> <span class="nx">value</span> <span class="nx">MUST</span> <span class="nx">BE</span> <span class="err">&gt;</span><span class="p">=</span> <span class="mi">5900</span> <span class="nx">and</span> <span class="err">&lt;</span><span class="p">=</span> <span class="mi">65535</span>
</span></span><span class="line"><span class="cl"><span class="nx">vnc_console_start_port_range</span> <span class="p">=</span> <span class="mi">5900</span>
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">Last</span> <span class="nx">VNC</span> <span class="nx">console</span> <span class="nx">port</span> <span class="nx">of</span> <span class="nx">the</span> <span class="nx">range</span> <span class="nx">allocated</span> <span class="nx">to</span> <span class="nx">devices</span>
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">The</span> <span class="nx">value</span> <span class="nx">MUST</span> <span class="nx">BE</span> <span class="err">&gt;</span><span class="p">=</span> <span class="mi">5900</span> <span class="nx">and</span> <span class="err">&lt;</span><span class="p">=</span> <span class="mi">65535</span>
</span></span><span class="line"><span class="cl"><span class="nx">vnc_console_end_port_range</span> <span class="p">=</span> <span class="mi">10000</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">First</span> <span class="nx">port</span> <span class="nx">of</span> <span class="nx">the</span> <span class="nx">range</span> <span class="nx">allocated</span> <span class="nx">for</span> <span class="nx">inter-device</span> <span class="nx">communication</span><span class="p">.</span> <span class="nx">Two</span> <span class="nx">ports</span> <span class="nx">are</span> <span class="nx">allocated</span> <span class="nx">per</span> <span class="nx">link</span><span class="p">.</span>
</span></span><span class="line"><span class="cl"><span class="nx">udp_start_port_range</span> <span class="p">=</span> <span class="mi">10000</span>
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">Last</span> <span class="nx">port</span> <span class="nx">of</span> <span class="nx">the</span> <span class="nx">range</span> <span class="nx">allocated</span> <span class="nx">for</span> <span class="nx">inter-device</span> <span class="nx">communication</span><span class="p">.</span> <span class="nx">Two</span> <span class="nx">ports</span> <span class="nx">are</span> <span class="nx">allocated</span> <span class="nx">per</span> <span class="nx">link</span>
</span></span><span class="line"><span class="cl"><span class="nx">udp_end_port_range</span> <span class="p">=</span> <span class="mi">20000</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">Only</span> <span class="nx">allow</span> <span class="nx">these</span> <span class="nx">interfaces</span> <span class="nx">to</span> <span class="nx">be</span> <span class="nx">used</span> <span class="nx">by</span> <span class="nx">GNS3</span><span class="p">,</span> <span class="nx">for</span> <span class="nx">the</span> <span class="nx">Cloud</span> <span class="nx">node</span> <span class="nx">for</span> <span class="nx">example</span> <span class="err">(</span><span class="nx">Linux</span><span class="err">/</span><span class="nx">OSX</span> <span class="nx">only</span><span class="err">)</span>
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">Do</span> <span class="nx">not</span> <span class="nx">forget</span> <span class="nx">to</span> <span class="nx">allow</span> <span class="nx">virbr0</span> <span class="nx">in</span> <span class="nx">order</span> <span class="nx">for</span> <span class="nx">the</span> <span class="nx">NAT</span> <span class="nx">node</span> <span class="nx">to</span> <span class="nx">work</span>
</span></span><span class="line"><span class="cl"><span class="nx">allowed_interfaces</span> <span class="p">=</span> <span class="nx">virbr0</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">Enable</span> <span class="nx">the</span> <span class="nx">built-in</span> <span class="nx">templates</span>
</span></span><span class="line"><span class="cl"><span class="nx">enable_builtin_templates</span> <span class="p">=</span> <span class="nx">True</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="err">;</span> <span class="nx">check</span> <span class="nx">if</span> <span class="nx">hardware</span> <span class="nx">virtualization</span> <span class="nx">is</span> <span class="nx">used</span> <span class="nx">by</span> <span class="nx">other</span> <span class="nx">emulators</span> <span class="err">(</span><span class="nx">KVM</span><span class="p">,</span> <span class="nx">VMware</span> <span class="nx">or</span> <span class="nx">VirtualBox</span><span class="err">)</span>
</span></span><span class="line"><span class="cl"><span class="nx">hardware_virtualization_check</span> <span class="p">=</span> <span class="nx">False</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">[</span><span class="nx">Dynamips</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="nx">allocate_aux_console_ports</span> <span class="p">=</span> <span class="nx">False</span>
</span></span><span class="line"><span class="cl"><span class="nx">mmap_support</span> <span class="p">=</span> <span class="nx">True</span>
</span></span><span class="line"><span class="cl"><span class="nx">sparse_memory_support</span> <span class="p">=</span> <span class="nx">True</span>
</span></span><span class="line"><span class="cl"><span class="nx">ghost_ios_support</span> <span class="p">=</span> <span class="nx">True</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">[</span><span class="nx">IOU</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="nx">iourc_path</span> <span class="p">=</span> <span class="err">/</span><span class="nx">opt</span><span class="err">/</span><span class="nx">gns3</span><span class="err">/</span><span class="nx">iourc</span>
</span></span><span class="line"><span class="cl"><span class="nx">license_check</span> <span class="p">=</span> <span class="nx">True</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">[</span><span class="nx">Qemu</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="nx">enable_kvm</span> <span class="p">=</span> <span class="nx">False</span>
</span></span><span class="line"><span class="cl"><span class="nx">require_kvm</span> <span class="p">=</span> <span class="nx">False</span>
</span></span><span class="line"><span class="cl"><span class="nx">enable_hardware_acceleration</span> <span class="p">=</span> <span class="nx">False</span>
</span></span><span class="line"><span class="cl"><span class="nx">require_hardware_acceleration</span> <span class="p">=</span> <span class="nx">False</span></span></span></code></pre>
</figure><p>Save it under  <code>/opt/gns3/.config/GNS3/2.2/gns3_server.conf</code>.</p>
<h3 id="start-and-daemonize-gns3">Start and Daemonize GNS3</h3>
<p>Create a GNS3 service file under <code>/etc/systemd/system/gns3_server.service</code>.</p>
<figure class="highlight">
    <figcaption>
        <span class="hl-filename">gns3-server.service</span>
    </figcaption>
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl"><span class="o">[</span>Unit<span class="o">]</span>
</span></span><span class="line"><span class="cl"><span class="nv">Description</span><span class="o">=</span>GNS3 server
</span></span><span class="line"><span class="cl"><span class="nv">Wants</span><span class="o">=</span>network-online.target
</span></span><span class="line"><span class="cl"><span class="nv">After</span><span class="o">=</span>network.target network-online.target
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">[</span>Service<span class="o">]</span>
</span></span><span class="line"><span class="cl"><span class="nv">User</span><span class="o">=</span>gns3-server
</span></span><span class="line"><span class="cl"><span class="nv">Group</span><span class="o">=</span>gns3-server
</span></span><span class="line"><span class="cl"><span class="nv">ExecStart</span><span class="o">=</span>/usr/bin/gns3server --config /opt/gns3/.config/GNS3/2.2/gns3_server.conf
</span></span><span class="line"><span class="cl"><span class="c1">#Restart=on-failure</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">[</span>Install<span class="o">]</span>
</span></span><span class="line"><span class="cl"><span class="nv">WantedBy</span><span class="o">=</span>multi-user.target</span></span></code></pre>
</figure><p>Create a symlink for <code>/usr/bin/gns3server</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># ln -s /opt/gns3/.local/share/pipx/venvs/gns3-server/bin/gns3server /usr/bin/gns3server </span></span></span></code></pre>
</figure><p>We enable GNS3 server on boot</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># systemctl enable gns3-server</span></span></span></code></pre>
</figure><p>Start GNS3 server and verify if it&rsquo;s running</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="line"><span class="cl">root@gns3 <span class="c1"># systemctl start gns3-server</span>
</span></span><span class="line"><span class="cl">root@gns3 <span class="c1"># systemctl status gns3-server</span></span></span></code></pre>
</figure><h2 id="gns3-client">GNS3 Client</h2>
<h3 id="setup-gns3-server">Setup GNS3 Server</h3>
<p>GNS3 Client should detect the server automatically, but if it&rsquo;s not, grab the Linux machine IP from OrbStack GUI.</p>
<figure>
    <img src="./imgs/image-20260305221846819.png"
         alt="OrbStack Machine IP"/> <figcaption>
            <p>OrbStack Machine IP
            </p>
        </figcaption>
</figure>

<p>Open <strong>the GNS3 Client &gt; Settings &gt; Server</strong> and point the GNS3 server to the Linux machine IP or its DNS.</p>
<figure>
    <img src="./imgs/image-20260305222207445.png"
         alt="Set GNS3 Server"/> <figcaption>
            <p>Set GNS3 Server
            </p>
        </figcaption>
</figure>

<h3 id="verify-client-setup">Verify Client Setup</h3>
<p>First, we create a project (you name it) and then check the project path we created in the <strong>Project &gt; Projects Library</strong>.</p>
<img src="./imgs/image-20260305223453028.png" alt="image-20260305223453028" style="zoom:50%;" />
<p>Make sure the project paths display <code>/opt/gns3/*</code> , this  means we have pointed the GNS3 client to GNS3 Server on the OrbStack machine correctly. If somehow you see path filled with something like <code>/Users/[username]/GNS3/projects</code>, try repeating the steps.</p>
<h3 id="appliances-installation">Appliances Installation</h3>
<p>The network appliances now can be installed normally.</p>
<h2 id="the-results">The results</h2>
<h3 id="testing-a-soho-project">Testing a SOHO project</h3>
<p>I&rsquo;ve tested the basic appliances by creating a simple SOHO project,everything works as expected.</p>
<figure>
    <img src="./imgs/image-20260306233242225.png"
         alt="Simple SOHO"/> <figcaption>
            <p>Simple SOHO
            </p>
        </figcaption>
</figure>

<p>Everything runs smoothly and fast, with no choppy performance at all.</p>
<h3 id="network-limitation-issue">Network Limitation Issue</h3>
<h4 id="accessing-gns3-appliances-from-mac-lan">Accessing GNS3 Appliances from Mac LAN</h4>
<p>Since I do most networking work with MikroTik and its GUI config tool called WinBox, I want the GNS3 network appliances to be able to communicate directly with the LAN on my Mac.</p>
<p>Orbstack Linux machines are connected to the same network bridge with a fixed IPv4 address of <code>198.19.249.0/24</code> or  <code>192.168.139.0/24</code> in my case. On my Mac, this network bridge displayed as <code>bridge100</code> and the GNS3 machine connected to that bridge via its <code>eth0</code>.</p>
<figure class="highlight">
    <pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">Macbook [bridge100]-------[eth0] GNS3 Server (host machine)
</span></span><span class="line"><span class="cl">                            |
</span></span><span class="line"><span class="cl">                          Cloud Node
</span></span><span class="line"><span class="cl">                            |
</span></span><span class="line"><span class="cl">                          [ethX] MikroTik Appliance</span></span></code></pre>
</figure><p>And to bridge GNS3 server network (host interfaces) with GNS3 virtual network, I would have to configure the <code>allowed_interfaces</code> option in the GNS3 configuration file.</p>
<figure class="highlight">
    <figcaption>
        <span class="hl-filename">gns3_server.conf</span>
    </figcaption>
    <pre tabindex="0" class="chroma"><code class="language-ini" data-lang="ini"><span class="line"><span class="cl"><span class="na">...</span>
</span></span><span class="line"><span class="cl"><span class="c1">; Only allow these interfaces to be used by GNS3, for the Cloud node for example (Linux/OSX only)</span>
</span></span><span class="line"><span class="cl"><span class="c1">; Do not forget to allow virbr0 in order for the NAT node to work</span>
</span></span><span class="line"><span class="cl"><span class="na">allowed_interfaces</span> <span class="o">=</span> <span class="s">virbr0,eth0 </span>
</span></span><span class="line"><span class="cl"><span class="na">...</span></span></span></code></pre>
</figure><p>Logically, my WinBox can now reach the MikroTik appliance through the Cloud node acting as a network bridge.</p>
<figure>
    <img src="./imgs/image-20260309012458452.png"
         alt="Accessing MikroTik appliance"/> <figcaption>
            <p>Accessing MikroTik appliance
            </p>
        </figcaption>
</figure>

<p>Indeed, pinging from Mac (<code>192.19.249.3</code>) to the Mikrotik appliance (<code>192.19.249.150</code>, assigned manually) is reachable.</p>
<img src="./imgs/image-20250201140538579.png" alt="image-20250201140538579" style="zoom: 33%;" />
<p>However, I&rsquo;m still unable to access any services such as MikroTik Winbox on port 8291 or even WebFig on port 80.</p>
<img src="./imgs/image-20250202135153641.png" alt="image-20250202135153641" style="zoom: 33%;" />
<p>Inspecting the traffic with Wireshark, I saw that no TCP handshake was performed between my Mac and the Mikrotik appliance.</p>
<img src="./imgs/image-20250201163513432.png" alt="image-20250201163513432" style="zoom: 33%;" />
<p>At this point, I just gave up.</p>
<p>Orbstack network implementation is beyond my understanding or it seems I have to wait for Orbstack to fully support bridged networking.</p>
<p>While I personally liked Orbstack more because of the experience it offers similar to WSL, in the meantime, I will look into other options like UTM or PNETLab (check <a href="/blog/deploy-pnetlab-on-gcp/"target="_blank" rel="noopener noreferrer"
>this out</a>).</p>
<h4 id="workaround">Workaround</h4>
<p>To make this case possible, I used L2TP VPN to bridge the GNS3 virtual network environment with my mac LAN with OSPF routing as a workaround.</p>
<h2 id="conclusion">Conclusion</h2>
<p>I&rsquo;m so impressed by how easy it is to deploy a Linux machine using OrbStack. It&rsquo;s like running a headless vm but in container ways. If we put the network limitation issue aside,  this is more than enough for simulating networking projects locally in your Apple Silicon Mac.</p>
<p>That&rsquo;s all, see you in the next post!</p>
]]></content:encoded>
    </item>
    
  </channel>
</rss>
